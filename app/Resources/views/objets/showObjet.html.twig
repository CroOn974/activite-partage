{% extends 'base.html.twig' %}

{% block body %}
    <div class="container m-t-3 p-t-1">
        <h1>Mes requetes</h1>

        <table class="table">
            <thead>
            <tr>
                <th>Nom de l'association et adresse</th>
                <th>Objet concerné</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            {% for objet in queryObjet %}
                <tr>
                    <td>{{ objet.reservation.0.association.nom }} <br> {{ objet.reservation.0.association.adresse }}</td>
                    <td>{{ objet.nom }}</td>
                    <td>
                        <p><a class="btn btn-success" href="">Accepter</a></p>
                        <p><a class="btn btn-danger" href="{{ path('delete_request', {'id':objet.id}) }}">Refuser</a>
                        </p>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
